// содержит конфигурационные настройки для логирования и разрешения хостов в приложении
{
  // определяет настройки для логирования
  "Logging": {
    // определяет уровни журналирования для различных категорий или областей приложения. 
    // Уровень журналирования определяет, какие сообщения будут записываться в журнал. 
    // Здесь определены два уровня журналирования: уровень по умолчанию (Default) 
    // и уровень журналирования для области Microsoft.AspNetCore
    "LogLevel": {
      // "Default": "Information": Данная строка определяет уровень журналирования 
      // по умолчанию для всего приложения. Уровень журналирования "Information" означает, 
      // что в журнал будут записываться сообщения с уровнями "Information", "Warning", "Error" 
      // и "Critical", но не "Debug" и "Trace".
      "Default": "Information",
      // Определяет уровень журналирования для области Microsoft.AspNetCore в приложении. 
      // Здесь установлен уровень "Warning", что означает, что в журнал будут записываться 
      // только сообщения с уровнем "Warning", "Error" и "Critical", 
      // но не "Information", "Debug" и "Trace".
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Определяет, какие хосты разрешены для приложения. Здесь символ * указывает на то, 
  // что все хосты разрешены. Это означает, что любой хост сможет обращаться к приложению 
  // без ограничений по имени хоста.
  "AllowedHosts": "*"
}
